version: '3'
services:
  dashboard:
    build: .
    ports:
      - "8080:80"
    environment:
      -  APP_MODULE=wsgi:app
    depends_on:
      - "mongo"
  mongo:
    image: "mongo:latest"
    ports:
      - "27017:27017"